/* Layout strings (Urdu) — the app at every width. Deep-merged over ur.js.

   "ایڈمن" is the word the admin area already uses (سپر ایڈمن), and
   "بنیادی حصے" is what the bottom bar is already called (hub.navLabel),
   so neither introduces a new term. */
export default {
  layout: {
    adminPanel: "ایڈمن پینل",
    skipToTabs: "بنیادی حصوں پر جائیں",
  },
};
